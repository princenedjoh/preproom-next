import { Inter, Playfair } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
 
export const playFair = Playfair({
  subsets: ['latin'],
  display: 'swap',
})